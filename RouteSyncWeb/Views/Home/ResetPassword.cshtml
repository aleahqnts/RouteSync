@model FleetWise.Models.ResetPasswordViewModel
@{
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
}

<div class="bg-wrapper">
    <div class="card">

        @* Left Panel *@
        <div class="left-panel">
            <div class="left-content">
                <img src="~/images/fleetwise-logo.png" alt="FleetWise Logo" class="logo" />
                <div class="brand-name">
                    <span class="brand-fleet">Route</span><span class="brand-wise">Sync</span>
                </div>
                <p class="brand-sub">Operator Dashboard</p>
                <p class="tagline">Drive Your Fleet with Intelligence</p>
            </div>
        </div>

        @* Right Panel *@
        <div class="right-panel">
            <h1 class="step-title">New Password</h1>

            <form asp-action="ResetPassword" asp-controller="Home" method="post">
                @Html.AntiForgeryToken()
                @if (ViewData.ModelState[string.Empty]?.Errors.Count > 0)
                {
                    <div class="error-message">
                        @Html.ValidationSummary(true)
                    </div>
                }

                <input type="hidden" asp-for="ResetToken" />

                @await Html.PartialAsync("_PasswordFields")
                <button type="submit" class="sign-in-btn">Save Password</button>
            </form>
        </div>

    </div>
</div>

@section Scripts {
    <script src="~/js/password-ui.js" asp-append-version="true"></script>
}
